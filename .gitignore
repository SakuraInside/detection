models/*.pt
models/*.engine
__pycache__/
*.pyc
logs/events.db
logs/events.db-*

# local virtual environment
.venv/

# large model weights (use releases/LFS if needed)
*.pt
runtime-core/target/
video-bridge/target/
native/build/

# local video data (too large for git)
data/*.mkv
data/*.mp4
data/*.avi
data/*.mov
data/*.webm
data/*.m4v

# Local / MSVC CMake output — все варианты build-директорий
native/build-msvc/
native/build-msvc-*/
native/build-trt/
native/build-trt-*/
native/build*/

# Claude Code worktrees (локальные, не для репо)
.claire/
.claude/

# OpenCV or other huge local build trees
/build/

# Runtime: snapshots, dumps, SQLite (не коммитить с рабочей машины)
logs/**/*.jpg
logs/**/*.jpeg
logs/**/*.png
logs/events*.db
logs/events*.db-shm
logs/events*.db-wal
logs/*.json
smoke-*.log

# ONNX веса часто сотни МБ — держите локально или через LFS
models/*.onnx
