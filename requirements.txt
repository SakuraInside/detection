# Integra-LOST — зависимости Python.
#
# run.py (launcher) — чистая stdlib, pip не требует.
#
# Режим --py-infer (pyinfer): инференс YOLOv11 через onnxruntime-gpu
# (CUDAExecutionProvider). torch ставится ради CUDA/cuDNN DLL — pyinfer
# подкладывает torch/lib в PATH, чтобы onnxruntime подцепил cublasLt/cudnn.
# Для отключения GPU: INTEGRA_FORCE_CPU=1.
#
# Установка: .venv\Scripts\python -m pip install -r requirements.txt

onnxruntime-gpu>=1.17    # или onnxruntime (CPU only)
torch>=2.5               # CUDA-сборка cu121 поставляет cudnn/cublasLt DLL
numpy>=1.26,<2
opencv-python>=4.9
lap>=0.5                  # линейное назначение для ByteTrack

# Только для офлайн-экспорта весов .pt -> .onnx (scripts/export_onnx.py):
# ultralytics>=8.4
